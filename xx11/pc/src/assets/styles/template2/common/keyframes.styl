//全局的动画类型
@keyframes jump
    0%
        transform: translateY(-5px)
    to
        transform: translateY(-2px)

@keyframes loading
    from
        transform rotate(0deg)
    to
        transform rotate(360deg)

